{
  "version": 2,
  "buildCommand": "cd font_end && npm install && npm run build",
  "outputDirectory": "font_end/build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
